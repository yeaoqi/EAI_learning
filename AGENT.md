# 项目协作与笔记规范

## 项目定位

本项目是面向具身智能（Embodied AI）的长期学习仓库，按周组织学习路线。学习笔记放在 `notes/`，可运行练习放在 `labs/`，阶段性成果放在 `projects/`。

## 周次归属

- 数学、统计、微积分、优化等基础内容放在 `notes/01-math/`。
- Week 1：线性代数与 SVD。
- Week 2：概率、统计与实验规范。
- Week 3：微积分与优化。
- 后续周次应先查看 `ROADMAP.md`，再决定对应主题目录。

## 笔记写作风格

新增周次笔记默认采用“自测问答”形式，而不是直接写成完整讲义：

每次新建周次 notes 前，必须先检查 `docs/index.html` 和 `ROADMAP.md` 中对应周次的学习内容、交付任务与验收要求；notes 的问题必须覆盖页面提到的全部核心主题和验收能力，不能只凭主题名称自由扩展。

1. 文件名使用类似 `03-calculus-optimization-my-0.md` 的格式，其中 `my-0` 表示第一次自测版本。
2. 开头说明本文件用于学习与自测，并提醒先独立作答。
3. 按 `A.`, `B.`, `C.` 等主题分组，再使用 `A1.`, `A2.` 等问题编号。
4. 每个小节先提出问题，问题后保留“答案：”和空白区域，不要预先填入完整答案。
5. 问题应覆盖：核心概念、公式推导、手算题、应用场景、常见误区和结果解释。
6. 数学公式使用 Markdown 行内代码或 fenced code block；变量命名保持清晰，避免无意义的单字母变量（数学公式中的标准变量除外）。
7. 结尾加入“本周验收”或类似自评部分，让学习者用自己的话总结掌握情况和遗留问题。

## 代码训练题规范

笔记中应包含与主题直接相关的代码题，通常使用 Python/NumPy：

- 用 `pass` 留出待实现函数，而不是直接给出实现。
- 题目明确输入、输出、边界条件和至少一组测试要求。
- 优先加入参数校验、形状校验、随机种子和可复现实验要求。
- 代码题后保留“答案：”区域，供完成实验后记录实现和结果。
- 实验应关注曲线、误差、稳定性、多个随机种子或不同超参数，而不是只展示一次最好结果。

## 修改与验证

- 修改应保持现有目录结构和命名风格，避免改动无关文件。
- 新增内容前先阅读同目录已有笔记，尤其是对应周次的 `*-my-0.md` 和参考答案文件。
- 完成后检查 Markdown 标题层级、代码块闭合和 Git 工作区变更。
- 除非用户明确要求，不要自动填写自测答案、创建提交或修改无关实验。
