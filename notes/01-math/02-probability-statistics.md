# 02 概率、统计与实验规范自测
本文件用于第二周学习与自测。建议先独立作答；不确定的地方标记为“待补充”，完成实验后再回来修正答案。

## A. 概率基础
### A1. 随机事件与概率
请用自己的话解释：

+ 什么是样本空间？
+ 什么是随机事件？
+ 概率为 0 或 1 是否一定分别表示“不可能”或“必然”？

答：



### A2. 联合概率、边缘概率与条件概率
请分别解释下面三个概念，并说明它们之间的关系：

+ 联合概率 `P(A, B)`
+ 边缘概率 `P(A)`
+ 条件概率 `P(A | B)`

答：



### A3. 独立与互斥
“事件 A 与事件 B 相互独立”和“事件 A 与事件 B 互斥”有什么区别？两个非零概率事件能否同时独立且互斥？

答：



### A4. 全概率公式
请写出全概率公式，并举一个机器人或感知场景中的例子。

答：



### A5. 贝叶斯公式
请写出贝叶斯公式，并解释其中的：

+ 先验概率
+ 似然
+ 后验概率
+ 证据或归一化常数

答：



### A6. 医疗检测问题
某种疾病在人群中的患病率是 1%。检测对患者呈阳性的概率为 95%，对健康人误报为阳性的概率为 5%。一个人检测结果为阳性时，他真正患病的概率是多少？

请先凭直觉判断，再使用贝叶斯公式计算。

答：



## B. 随机变量与常见分布
### B1. 随机变量
随机变量与普通程序变量有什么不同？离散随机变量和连续随机变量有什么区别？

答：



### B2. 概率质量函数与概率密度函数
请解释：

+ PMF 和 PDF 分别适用于什么随机变量？
+ 为什么连续随机变量在某个精确点上的概率通常为 0，但该点的概率密度可以大于 0？
+ CDF 表示什么？

答：



### B3. 伯努利分布与二项分布
伯努利分布和二项分布有什么关系？请分别举一个使用场景。

答：



### B4. 高斯分布
高斯分布由哪些参数决定？这些参数分别控制什么？为什么实验噪声经常被近似为高斯分布？

答：



### B5. Beta 分布
Beta 分布的取值范围是什么？为什么它适合表示硬币正面概率等未知概率参数的先验？参数 `alpha` 和 `beta` 大致表达什么？

答：



### B6. 分布选择
请为下列变量选择一个可能适合的分布，并说明理由：

+ 一次抓取是否成功
+ 100 次抓取中的成功次数
+ 相机测量误差
+ 每分钟收到的故障消息数量

答：



## C. 期望、方差与协方差
### C1. 期望
期望表示什么？期望是否一定是随机变量可能实际取得的值？请举例说明。

答：



### C2. 方差与标准差
请解释方差和标准差分别衡量什么。为什么实验报告通常更容易解释标准差，而计算过程中经常使用方差？

答：



### C3. 线性变换
若随机变量 `X` 的均值为 `mu`、方差为 `sigma^2`，定义 `Y = aX + b`。请写出 `Y` 的均值和方差。

答：



### C4. 协方差与相关系数
请解释：

+ 协方差的正负分别表示什么？
+ 协方差为什么受量纲影响？
+ 相关系数相比协方差有什么优点？
+ 相关系数为 0 是否一定表示两个变量独立？

答：



### C5. 样本统计量
总体均值、样本均值、总体方差和样本方差有什么区别？为什么无偏样本方差常使用 `n - 1` 而不是 `n`？

答：



## D. MLE 与 MAP
### D1. 似然
概率和似然有什么区别？当观测数据已经固定时，似然函数把什么看作变量？

答：



### D2. 最大似然估计
请用自己的话解释 MLE 想寻找什么。为什么实际推导中经常最大化对数似然而不是原始似然？

答：



### D3. 硬币的 MLE
一枚硬币独立抛掷 `n` 次，其中 `k` 次为正面。假设正面概率为 `p`：

1. 写出观测数据的似然函数。
2. 写出对数似然函数。
3. 推导 `p` 的最大似然估计。

答：



### D4. 最大后验估计
请解释 MAP 与 MLE 的区别。数据很少和数据很多时，先验对 MAP 结果的影响分别如何？

答：



### D5. Beta-Bernoulli MAP
假设硬币正面概率的先验为 `Beta(alpha, beta)`，观察到 `k` 次正面和 `n-k` 次反面：

1. 后验分布是什么？
2. 后验参数如何变化？
3. MAP 估计是什么？
4. 当使用均匀先验时，MAP 与 MLE 有什么关系？

答：



### D6. MLE、MAP 与过拟合
为什么在数据量很少时，MLE 可能得到过于极端的估计？先验如何起到正则化作用？

答：



## E. 抽样、置信区间与实验结果
### E1. 总体与样本
什么是总体、样本和抽样？为什么我们通常只能通过样本推测总体？

答：



### E2. 大数定律
大数定律大致说明了什么？它是否意味着样本量增大后每一次观测都会更接近期望？

答：



### E3. 中心极限定理
中心极限定理大致说明了什么？它为什么对实验均值和置信区间有用？

答：



### E4. 标准差与标准误
标准差和均值的标准误有什么区别？当样本量增加时，它们通常分别如何变化？

答：



### E5. 置信区间
请用自己的话解释“均值的 95% 置信区间”。下面哪一种说法更准确，为什么？

1. 当前区间有 95% 的概率包含真实均值。
2. 如果重复抽样并使用相同方法构造区间，长期来看约 95% 的区间会包含真实均值。

答：



### E6. 置信区间宽度
下列因素会如何影响置信区间宽度？

+ 样本量增加
+ 实验波动增大
+ 置信水平从 95% 提高到 99%

答：



### E7. 多随机种子实验
为什么训练模型或运行随机算法时，不能只报告最好的一次结果？至少应该报告哪些统计量？

答：



### E8. 统计显著性与实际价值
“差异具有统计显著性”是否一定说明改进很有实际价值？请解释统计显著性、效应大小和实际价值之间的区别。

答：



## F. 实验规范与可复现性
### F1. 可复现的实验
一个实验要能够复现，至少需要记录哪些信息？请从代码、环境、数据、配置和随机性几个方面回答。

答：



### F2. 随机种子
固定随机种子有什么作用？固定种子是否能够保证所有硬件和所有运行环境下得到完全相同的结果？

答：



### F3. 对照实验
什么是基线和对照组？比较两个方法时，哪些条件应该保持一致？

答：



### F4. 数据泄漏
什么是训练集、验证集和测试集？哪些操作可能造成数据泄漏？为什么测试集不应该被反复用于调参？

答：



### F5. 指标选择
假设一个机器人抓取系统在 100 次测试中成功 90 次。除了成功率之外，还应该记录哪些信息，才能更完整地判断系统表现？

答：



### F6. 失败样例
为什么实验报告不仅要保存成功案例，还要记录失败案例？如何对失败进行分类？

答：



### F7. 实验假设
请把“我的新方法应该更好”改写成一个具体、可测量、可证伪的实验假设。

答：



## G. NumPy 编程理解题
### G1. 描述性统计
阅读下面代码，并解释每个输出表示什么：

```python
import numpy as np

scores = np.array([0.72, 0.81, 0.76, 0.85, 0.79])

print(np.mean(scores))
print(np.var(scores, ddof=1))
print(np.std(scores, ddof=1))
print(np.median(scores))
```

问题：

+ `ddof=1` 有什么作用？
+ 均值和中位数分别容易受到什么因素影响？
+ 方差与标准差的单位有什么区别？

答：



### G2. 随机数生成器
阅读下面代码：

```python
import numpy as np

rng1 = np.random.default_rng(42)
rng2 = np.random.default_rng(42)

x1 = rng1.normal(loc=0.0, scale=1.0, size=5)
x2 = rng2.normal(loc=0.0, scale=1.0, size=5)

print(x1)
print(x2)
```

问题：

+ `loc`、`scale` 和 `size` 分别表示什么？
+ `x1` 和 `x2` 是否相同？为什么？
+ 为什么推荐为实验创建独立的随机数生成器？

答：



### G3. 模拟伯努利试验
补全一个函数，模拟成功概率为 `p` 的 `n` 次独立试验，并返回成功率：

```python
import numpy as np

def simulate_success_rate(p, n, seed):
    # 在这里完成代码
    pass
```

要求：

+ 使用 `np.random.default_rng(seed)`。
+ 检查 `p` 是否位于 `[0, 1]`。
+ 检查 `n` 是否为正整数。
+ 分别用 `n=10`、`100`、`10000` 运行并比较结果。

答：



### G4. 手动计算统计量
不直接调用 `np.mean`、`np.var` 和 `np.std`，实现以下函数：

```python
def sample_statistics(values):
    """返回样本均值、无偏样本方差和样本标准差。"""
    pass
```

用 NumPy 的结果验证你的实现，并处理样本数量小于 2 的情况。

答：



## H. 编程与实验题
### H1. MLE 与 MAP 硬币实验
编写程序估计一枚硬币的正面概率。程序至少应当：

1. 使用指定真实概率生成伯努利观测。
2. 计算 MLE。
3. 使用至少两组不同的 Beta 先验计算 MAP。
4. 比较样本量为 `5`、`20`、`100`、`1000` 时的估计结果。
5. 用表格或曲线展示真实值、MLE 和 MAP。

思考：

+ 小样本时哪种估计更容易受先验影响？
+ 如果先验严重错误，会发生什么？
+ 数据增多后 MLE 与 MAP 是否逐渐接近？

实验结论：



### H2. 多随机种子实验
选择一个存在随机性的简单实验，例如估计硬币正面概率。使用至少 10 个不同随机种子重复运行。

要求报告：

+ 每次运行的结果
+ 均值
+ 无偏样本标准差
+ 最小值和最大值
+ 最好的一次结果与平均结果的差异

思考：为什么只挑最好的一次会误导读者？

实验结论：



### H3. 均值的置信区间
实现计算均值 95% 置信区间的函数：

```python
def mean_confidence_interval(values, confidence=0.95):
    """返回样本均值、置信区间下界和上界。"""
    pass
```

要求：

+ 说明你使用的是正态近似还是 t 分布。
+ 不允许只复制公式，必须解释公式中每一项的意义。
+ 使用模拟数据检查样本量和方差对区间宽度的影响。
+ 如果使用第三方库，记录库名和版本。

实验结论：



### H4. 置信区间覆盖率模拟
从一个已知均值的分布中反复抽样，并为每次样本计算 95% 置信区间。统计这些区间中有多少包含真实均值。

建议设置：

+ 重复次数至少 1000 次。
+ 比较不同样本量。
+ 固定顶层随机种子。
+ 报告实际覆盖率，并解释它为什么不会刚好等于 95%。

实验结论：



### H5. 可复现实验检查
为本周的一个程序加入以下功能：

+ 命令行参数或集中配置
+ 随机种子设置
+ 参数合法性检查
+ 自动保存实验结果
+ 输出运行环境与依赖版本
+ 保存本次 Git commit ID

然后让同一程序以完全相同的配置运行两次，检查结果是否一致。

实验结论：



## I. 结果分析题
### I1. 比较两种方法
方法 A 的五次成功率为：

```latex
0.80, 0.82, 0.78, 0.81, 0.79
```

方法 B 的五次成功率为：

```latex
0.72, 0.91, 0.70, 0.93, 0.69
```

请分别计算两种方法的均值和样本标准差，并回答：

+ 哪种方法平均表现更好？
+ 哪种方法更稳定？
+ 仅比较最好结果会得到什么结论？
+ 当前数据是否足以判断哪种方法更值得部署？还缺少什么信息？

答：



### I2. 发现实验问题
某报告写道：

> 新方法在测试集上反复调参后取得了 93% 的最好成功率，基线使用默认参数运行一次取得了 88%，所以新方法提升了 5%。
>

请找出其中存在的实验设计问题，并提出改进方案。

答：



### I3. 解读重叠的置信区间
两个方法的 95% 置信区间发生重叠，是否一定说明它们没有显著差异？如果要进行更严谨的比较，还需要考虑什么？

答：



## J. 本周验收
完成本文件和配套实验后，请用自己的话总结：

+ 我现在理解条件概率和贝叶斯公式在解决什么问题。
+ 我可以解释 MLE 与 MAP 的区别。
+ 我可以区分方差、标准差、标准误和置信区间。
+ 我知道为什么随机实验需要运行多个种子。
+ 我能够记录并复现一次实验。
+ 我还不理解的地方是：

答：



## K. 建议交付物
完成 Week 2 时，建议至少保留以下证据：

+ 本文件中的自测答案。
+ 一个 MLE/MAP 编程实验。
+ 一个多随机种子与置信区间实验。
+ 一份根据 `templates/experiment-log.md` 填写的实验日志。
+ 一份 Week 2 周复盘。

