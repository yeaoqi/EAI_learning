# 04 SO(3) 与 SE(3) 自测

本文件用于第四周学习与自测。建议先独立作答；不确定的地方标记为“待补充”，完成代码实验后再回来修正答案。

本周目标：理解三维旋转与刚体位姿的常用表示，掌握 `SO(3)`、`SE(3)` 及其 `exp/log` 映射，能够实现带测试的旋转转换库，并解释欧拉角的 gimbal lock。

## A. 坐标系与旋转基础

### A1. 点、向量与坐标表示

“几何向量”和“向量在某个坐标系下的坐标”有什么区别？同一个空间点在坐标系 `{A}` 和 `{B}` 下为什么会有不同的数值？

答案：


### A2. 主动旋转与被动变换

请解释主动旋转和被动坐标变换的区别。为什么二者可能使用互为转置的旋转矩阵？阅读公式时如何判断作者采用的是哪一种约定？

答案：


### A3. 旋转矩阵的约束

三维矩阵 `R` 属于 `SO(3)` 需要满足哪些条件？请解释：

- 为什么 `R^T R = I` 表示保持长度和夹角；
- 为什么还要要求 `det(R)=1`；
- 为什么 `R^{-1}=R^T`。

答案：


### A4. 旋转矩阵手算

写出绕 `x`、`y`、`z` 轴旋转角度 `theta` 的基本旋转矩阵。令点 `point=[1, 0, 0]^T` 绕 `z` 轴主动旋转 `90°`，手算旋转后的坐标，并检查长度是否保持不变。

答案：


### A5. 旋转复合与顺序

设 `R_ab` 把 `{B}` 中的坐标转换到 `{A}`，`R_bc` 把 `{C}` 中的坐标转换到 `{B}`。请推导 `R_ac`，并解释为什么矩阵乘法顺序不能随意交换。

答案：


### A6. 相对旋转

已知物体的两个姿态 `R_world_1` 和 `R_world_2`，如何求从姿态 1 到姿态 2 的相对旋转？请明确你采用的坐标变换约定，并通过代入关系推导，而不是只写结论。

答案：


## B. 欧拉角、轴角与四元数

### B1. 欧拉角约定

什么是欧拉角？内旋与外旋、`XYZ` 与 `ZYX` 分别表示什么？为什么只给出三个角度而不说明顺序是不完整的？

答案：


### B2. Gimbal lock

以常见的 `ZYX` 欧拉角为例，解释 pitch 接近 `±90°` 时发生了什么。gimbal lock 是物体失去了一个真实旋转自由度，还是欧拉角参数化出现了奇异性？它会带来哪些数值或控制问题？

答案：


### B3. 轴角表示与 Rodrigues 公式

轴角表示由哪些量组成？写出单位旋转轴 `axis` 的反对称矩阵 `[axis]_x`，并使用 Rodrigues 公式表示旋转矩阵：

```text
R = I + sin(theta)[axis]_x + (1-cos(theta))[axis]_x^2
```

说明 `theta=0` 时公式为什么回到单位矩阵。

答案：


### B4. 反对称矩阵与叉乘

对 `vector=[v1, v2, v3]^T` 写出 `[vector]_x`，并验证对任意 `point` 都有：

```text
[vector]_x point = vector × point
```

答案：


### B5. 四元数含义

单位四元数可写为 `quaternion=[w, x, y, z]`。请解释实部、虚部与轴角的关系，并写出单位四元数约束。不同库为什么可能采用 `[w,x,y,z]` 或 `[x,y,z,w]` 两种顺序？

答案：


### B6. 四元数的双覆盖

为什么 `quaternion` 与 `-quaternion` 表示同一个旋转？比较两个四元数或插值前为什么需要处理符号？直接比较四元数分量可能产生什么误判？

答案：


### B7. 四元数复合与旋转

写出两个四元数的 Hamilton 乘积。说明四元数乘法为什么不可交换，以及如何用单位四元数旋转三维向量。

答案：


### B8. 旋转表示比较

比较旋转矩阵、欧拉角、轴角和单位四元数在参数数量、约束、奇异性、复合效率、插值和可解释性方面的优缺点。机器人系统中哪些场景更适合使用哪种表示？

答案：


## C. SE(3) 与齐次变换

### C1. 从 SO(3) 到 SE(3)

`SO(3)` 和 `SE(3)` 分别表示什么？为什么三维刚体位姿不仅需要旋转，还需要平移？二者各有多少个自由度？

答案：


### C2. 齐次变换矩阵

写出齐次变换：

```text
T = [[R, translation],
     [0,             1]]
```

说明各块的形状和含义。为什么把三维点补成 `[x,y,z,1]^T` 后，旋转和平移可以统一为一次矩阵乘法？方向向量应补 `0` 还是 `1`，为什么？

答案：


### C3. 位姿变换手算

坐标系 `{B}` 相对 `{A}` 绕 `z` 轴旋转 `90°`，且 `{B}` 原点在 `{A}` 中的位置为 `[1,2,0]^T`。构造 `T_ab`，并将 `{B}` 中的点 `[1,0,0]^T` 转换到 `{A}`。

答案：


### C4. 齐次变换的逆

从 `point_a = R_ab point_b + translation_ab` 出发，推导：

```text
T_ab^{-1} = [[R_ab^T, -R_ab^T translation_ab],
             [0,                                1]]
```

为什么逆变换的平移部分不是简单的 `-translation_ab`？

答案：


### C5. 变换链与 TF

已知 `T_world_base`、`T_base_camera` 和相机系中的点 `point_camera`，写出 `point_world`。如果交换矩阵相乘顺序，几何意义发生了什么变化？这与 ROS 2 的 TF 坐标树有什么联系？

答案：


### C6. 位姿不能直接逐元素平均

为什么多个旋转矩阵不能简单逐元素平均后直接当作旋转矩阵？为什么欧拉角在跨越 `±pi` 时直接求均值会出错？请给出至少一种更合理的旋转平均思路。

答案：


## D. 李群、李代数与 exp/log

### D1. 群的直观理解

结合旋转或位姿变换，解释群的封闭性、结合律、单位元和逆元。为什么 `SO(3)` 和 `SE(3)` 被称为李群？

答案：


### D2. so(3) 与 hat/vee

`so(3)` 中的元素是什么形式？写出三维向量与反对称矩阵之间的 hat、vee 映射，并解释 `so(3)` 为什么可以看作 `SO(3)` 在单位元附近的切空间。

答案：


### D3. SO(3) 指数映射

从矩阵指数：

```text
R = Exp([rotation_vector]_x)
```

说明旋转向量的方向和模长分别代表什么，并解释它与 Rodrigues 公式的关系。旋转向量接近零时，直接计算为什么需要小角度展开？

答案：


### D4. SO(3) 对数映射

对给定旋转矩阵 `R`，写出由 `trace(R)` 求旋转角的关系，并说明 `Log(R)` 返回什么。为什么旋转角接近 `0` 或 `pi` 时需要特殊数值处理？

答案：


### D5. se(3) 与 twist

一个刚体 twist 通常由角速度部分和线速度部分组成。写出六维 twist 的矩阵形式，并说明它与 `se(3)` 的关系。注意说明你采用 `[omega, velocity]` 还是 `[velocity, omega]` 排列约定。

答案：


### D6. SE(3) 指数与对数映射

说明 `Exp: se(3) -> SE(3)` 和 `Log: SE(3) -> se(3)` 的输入、输出及直观意义。它们为什么适合用于位姿插值、优化增量和误差定义？

答案：


### D7. 位姿误差定义

给定估计位姿 `T_estimated` 和目标位姿 `T_target`，可以构造左误差或右误差。请任选一种约定，写出相对变换以及通过 `Log` 得到六维误差的过程，并解释误差表达在哪个坐标系中。

答案：


### D8. 常见误区

逐项判断并修正：

1. 旋转向量就是欧拉角。
2. 任意 `3 x 3` 矩阵都是旋转矩阵。
3. 四元数只要有四个分量就能表示合法旋转。
4. 位姿变换矩阵可以像普通向量一样相加。
5. `Log(Exp(twist))` 对任意大小的 twist 都唯一等于原 twist。

答案：


## E. NumPy 代码训练题

### E1. SO(3) 基础工具与性质测试

补全以下函数：

```python
import numpy as np


def skew(vector):
    """输入 shape=(3,)，返回 shape=(3,3) 的反对称矩阵。"""
    pass


def project_to_so3(matrix):
    """用 SVD 把接近旋转矩阵的 3x3 矩阵投影到 SO(3)。"""
    pass


def is_rotation_matrix(matrix, atol=1e-9):
    """检查形状、正交性和行列式。"""
    pass
```

要求：

- 拒绝错误形状和非有限数值；
- 使用固定随机种子生成至少 `1000` 个随机测试；
- 验证 `R^T R≈I`、`det(R)≈1`、`R^{-1}≈R^T`；
- 给合法旋转矩阵加入小噪声，再检查投影后的性质；
- 报告最大绝对误差，而不是只写“测试通过”。

答案：


### E2. 旋转表示转换库

补全并测试以下转换函数：

```python
def axis_angle_to_matrix(axis, angle):
    pass


def matrix_to_axis_angle(matrix):
    pass


def quaternion_to_matrix(quaternion):
    pass


def matrix_to_quaternion(matrix):
    pass
```

要求：

- 明确四元数使用 `[w,x,y,z]` 还是 `[x,y,z,w]`；
- 检查旋转轴非零、四元数可归一化、矩阵属于 `SO(3)`；
- 测试角度接近 `0`、接近 `pi` 以及一般随机旋转；
- 往返转换应比较旋转矩阵或相对旋转角，不能直接要求四元数分量相等；
- 固定随机种子测试至少 `1000` 次，并报告最大旋转误差。

答案：


### E3. SE(3) 变换库与单元测试

补全以下函数：

```python
def make_transform(rotation, translation):
    pass


def inverse_transform(transform):
    pass


def transform_points(transform, points):
    """points 支持 shape=(3,) 或 shape=(n,3)。"""
    pass


def compose_transforms(transform_ab, transform_bc):
    pass
```

要求：

- 校验所有输入形状与有限性；
- 分别测试单点和批量点；
- 验证 `T @ inverse_transform(T)≈I`；
- 验证连续变换与复合变换结果一致；
- 使用随机位姿和随机点进行至少 `1000` 次可复现测试；
- 报告点坐标最大绝对误差。

答案：


### E4. exp/log 数值实验

使用自己实现的函数或可靠库完成以下实验：

```python
def so3_exp(rotation_vector):
    pass


def so3_log(rotation):
    pass
```

要求：

- 测试 `norm(rotation_vector)` 分别接近 `0`、一般角度和接近 `pi` 的情况；
- 比较 `Exp(Log(R))` 与 `R`；
- 比较 `Log(Exp(rotation_vector))` 时正确处理旋转表示的非唯一性；
- 绘制误差随旋转角变化的曲线；
- 解释误差在哪些角度附近变大以及原因。

答案：


### E5. 坐标系变换可视化

使用 Matplotlib 的三维坐标轴，可视化世界坐标系、两个子坐标系及一个点经过变换前后的结果。

要求：

- 每个坐标系分别画出红、绿、蓝三根轴；
- 标注坐标系名称和点坐标；
- 至少包含两级不交换的变换链；
- 用图和数值同时验证复合顺序；
- 保存随机种子、变换参数和输出图片，保证结果可复现。

答案：


### E6. Gimbal lock 数值观察

固定 yaw 和 roll，令 `ZYX` 欧拉角中的 pitch 从 `80°` 变化到 `100°`。将欧拉角转换为旋转矩阵后再转换回来，记录恢复出的三个角度。

要求：

- 绘制输入角和恢复角随 pitch 的变化曲线；
- 验证对应旋转矩阵是否仍然连续；
- 解释为什么欧拉角数值可能突变但实际旋转没有突然跳变；
- 说明这对姿态插值、控制器状态和日志分析有什么影响。

答案：


## F. 应用与结果解释

### F1. 机器人坐标变换

机械臂基座、末端执行器、相机和目标物体各自有坐标系。请设计一条从相机观测目标位姿到基座系目标位姿的变换链，并说明每个变换应通过标定、运动学还是感知获得。

答案：


### F2. 表示选择

分别为以下场景选择旋转或位姿表示，并解释原因：

1. 网络回归一个物体朝向；
2. 在配置文件中记录人类可读姿态；
3. 连续平滑插值两个朝向；
4. 计算优化器中的小位姿增量；
5. 在 TF 树中传递完整刚体位姿。

答案：


### F3. 调试检查清单

当变换后的点位置明显错误时，给出一套排查顺序，至少覆盖：坐标系命名、变换方向、乘法顺序、角度单位、欧拉角顺序、四元数分量顺序、矩阵存储约定和时间戳。

答案：


### F4. 数值误差与验收

“随机测试误差达到数值精度”具体应如何定义？为什么只测试单位矩阵或少量手工样例不够？说明你会记录哪些误差指标、边界样例和失败样例。

答案：


## G. 本周验收

完成本文件和代码实验后，请用自己的话回答：

1. 我能否解释 `SO(3)`、`SE(3)`、`so(3)` 和 `se(3)` 的区别与联系？
2. 我能否在明确坐标系和方向的前提下，独立写出并检查一条位姿变换链？
3. 我能否在旋转矩阵、欧拉角、轴角和四元数之间做可靠转换，并处理边界情况？
4. 我能否解释 gimbal lock 是参数化奇异性，而不是刚体失去真实自由度？
5. 我的随机往返测试是否达到预先设定的数值误差阈值，并记录随机种子和最大误差？
6. 我能否画出多坐标系变换图，并让图示结果与数值计算一致？
7. 我还不理解或需要继续验证的地方是什么？

答案：

