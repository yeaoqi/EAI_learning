# 01 线性代数与 SVD 自测

本文件用于第一周自测。建议先独立作答，不会的地方先标记，再回头查资料补齐。

## A. 向量与矩阵基础

### A1. 向量是什么？

请用自己的话解释：向量可以表示哪些东西？在几何上和数据上分别怎么理解？

答：


### A2. 矩阵是什么？

请分别从两个角度解释矩阵：

- 作为一组数字表格
- 作为一个线性变换

答：


### A3. 矩阵乘法的含义

为什么矩阵乘法 `AB` 不能简单理解成两个表格逐元素相乘？它在“线性变换复合”中代表什么？

答：


### A4. 线性相关与秩

请解释：

- 什么是线性相关？
- 什么是矩阵的秩？
- 秩不足时，方程组 `Ax = b` 可能出现哪些情况？

答：


### A5. 点积

两个向量的点积可以用来衡量什么？如果两个向量点积为 0，说明什么？

答：


## B. 最小二乘

### B1. 为什么需要最小二乘？

当方程组 `Ax = b` 没有精确解时，最小二乘想找到什么样的 `x`？

答：


### B2. 最小二乘最小化的是什么？

请解释下面这个目标函数的含义：

```text
min ||Ax - b||^2
```

答：


### B3. 直线拟合

假设你有一组二维点 `(x_i, y_i)`，想拟合直线：

```text
y = ax + b
```

请写出如何把它整理成 `Ax = b` 的形式。这里的未知量是什么？

答：


### B4. 法方程

普通最小二乘常见解法是：

```text
x = (A^T A)^(-1) A^T b
```

请解释为什么这个公式在数值上可能不稳定。

答：


## C. 特征值、特征向量与 SVD

### C1. 特征向量

如果 `Av = λv`，请解释：

- `v` 是什么？
- `λ` 是什么？
- 这个等式在几何上说明了什么？

答：


### C2. SVD 分解

SVD 的形式是：

```text
A = UΣV^T
```

请解释 `U`、`Σ`、`V^T` 大致分别代表什么。

答：


### C3. 奇异值

奇异值越大，通常说明矩阵在对应方向上做了什么？奇异值接近 0 又意味着什么？

答：


### C4. SVD 与秩

如何通过奇异值判断矩阵的有效秩？

答：


### C5. SVD 与最小二乘

为什么 SVD 可以用来解最小二乘问题？它相比直接求 `(A^T A)^(-1)` 有什么好处？

答：


## D. 机器人与感知中的应用

### D1. 坐标变换

机器人里经常用矩阵表示坐标变换。请举一个例子：什么时候需要把一个点从一个坐标系变换到另一个坐标系？

答：


### D2. 标定与最小二乘

相机标定、手眼标定或传感器标定中，为什么常常会出现“没有精确解，只能找最优解”的情况？

答：


### D3. SVD 在感知中的用途

请列出至少两个 SVD 可能出现的场景，例如 PCA、点云配准、低秩近似、去噪等，并简单解释。

答：


### D4. 噪声与误差

如果采集的数据有噪声，为什么不能只用一两个样本直接求解？更多样本和最小二乘有什么关系？

答：


## E. 代码理解题

### E1. NumPy 最小二乘

下面代码大概在做什么？

```python
import numpy as np

xs = np.array([0, 1, 2, 3, 4])
ys = np.array([1.1, 2.0, 2.9, 4.2, 5.1])

A = np.column_stack([xs, np.ones_like(xs)])
theta, residuals, rank, s = np.linalg.lstsq(A, ys, rcond=None)

print(theta)
print(residuals)
print(rank)
print(s)
```

请解释：

- `theta` 里两个数分别代表什么？
- `rank` 是什么？
- `s` 是什么？

答：


### E2. SVD 代码

下面代码中 `full_matrices=False` 有什么作用？你可以先猜，再查资料验证。

```python
U, S, Vt = np.linalg.svd(A, full_matrices=False)
```

答：


## F. 小练习

### F1. 手算

给定三个点：

```text
(0, 1)
(1, 3)
(2, 5)
```

这三个点是否刚好落在一条直线上？如果是，请写出直线方程。

答：


### F2. 思考

如果第三个点变成 `(2, 4.8)`，你还能找到一条精确通过所有点的直线吗？如果不能，最小二乘会怎么处理？

答：


## G. 本周验收

完成本文件后，请用自己的话总结：

- 我现在理解最小二乘是在解决什么问题。
- 我现在理解 SVD 为什么有用。
- 我还不理解的地方是：

答：

