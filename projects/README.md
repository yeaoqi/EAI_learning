# 阶段项目

每个 `milestone-*` 目录对应 52 周路线中的一个验收门。建议每个阶段项目都至少包含：

- `README.md`：目标、环境、运行方式和结果摘要
- `report.md`：实验报告或阶段复盘
- `src/`：核心代码
- `figures/`：图表和截图
- `logs/`：可分享的关键日志摘要

大体积数据、模型权重和原始实验输出不要直接提交到 Git。可以在 README 中记录下载地址、生成方式或外部存储位置。
